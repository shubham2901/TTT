---
description: TTT — fast mode (Vibe it); assumption-heavy spec in one pass.
---

# /ttt-vibe-it

Load **`skills/ttt-vibe-it/SKILL.md`**. Updates `ttt_state.json` with `vibe_it.used: true`. Artifacts under `{artifact_root}` (see state).
