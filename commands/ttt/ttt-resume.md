---
description: TTT — resume session from ttt_state.json.
---

# /ttt-resume

Load **`skills/ttt-resume/SKILL.md`**. Reads `{artifact_root}/ttt_state.json` and continues the flow. Full behavior defers to **`skills/ttt-new-idea`** choreographer where not specified.
