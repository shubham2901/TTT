---
phase: 00-runtime-gates
plan: 02
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - docs/PHASE0_DECISIONS.md
  - docs/phase0-spike-log.md
autonomous: true
requirements:
  - p0-web-search
  - p0-artifact-paths
  - p0-staggered-ux

must_haves:
  truths:
    - Runtime × search capability × user fallback matrix exists.
    - Skip Validate is documented as opt-in with user acknowledgment and ttt_state recording.
    - After one search-blocked researcher failure, escalation to user is documented (not full retry burn).
    - Default artifact root, collision fallback, backup-before-overwrite, and monorepo path pattern are fixed in a table.
    - Staggered execution uses honest ordering; Cursor vs Claude may differ in surface wording only.
  artifacts:
    - docs/PHASE0_DECISIONS.md (expanded)
    - docs/phase0-spike-log.md (Web search subsection)
  key_links:
    - Artifact paths align with `plan.md` output file table.
---

<objective>
Lock **web search / degradation** behavior and **artifact path** conventions per user CONTEXT, and document **staggered subagent** UX — building on plan 01.

Purpose: Satisfy P0-T3, P0-T4, and CONTEXT decisions on search, paths, and parallel vs staggered messaging.
Output: Updated `docs/PHASE0_DECISIONS.md` with § Web search, § Paths, § Execution ordering; spike log note for search probes.
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-runtime-gates/00-CONTEXT.md
@plan.md
@PHASE_PLAN.md
@ttt_master_architecture_part2.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Web search matrix and degradation rules</name>
  <files>docs/PHASE0_DECISIONS.md, docs/phase0-spike-log.md</files>
  <action>In PHASE0_DECISIONS, add **§ Web search** with a table: Runtime (Cursor | Claude Code) × Search/browse available (yes / partial / no) × **User fallback** (paste sources, skip Validate with ack, etc.). Encode CONTEXT exactly: brief factual tone when degrading; keep full research section structure with N/A in outputs; **skip Validate** only with explicit user acknowledgment recorded in `ttt_state.json`; **one** failure when search is the blocker → escalate to user (paste sources or mode switch), not three generic retries. Append a short **Web search** subsection to phase0-spike-log with dated notes from probes or ASSUMPTION.</action>
  <verify>PHASE0_DECISIONS contains a markdown table with both runtimes and a paragraph referencing skip-validate + ack + fast escalation.</verify>
  <done>P0-T3 satisfied at document level; consistent with CONTEXT.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact paths and collision / backup / monorepo</name>
  <files>docs/PHASE0_DECISIONS.md</files>
  <action>Add **§ Paths** with: (1) **Default root** — `ttt/` at chosen project/package root (pick `ttt/` as primary default per CONTEXT; mention `docs/ttt/` as optional variant in maintainer notes). (2) **Collision**: if `ttt/` exists and is unrelated, auto-fallback order e.g. `.ttt/` then `ttt-artifacts/` — state how choreographer records resolved path in `ttt_state.json`. (3) **Backup before overwrite**: `ttt/_backup/<ISO-8601-timestamp>/` copying prior artifact bundle before regenerating. (4) **Monorepo**: table mapping “single product thesis” vs per-package `apps/<pkg>/ttt/` with state storing active root. (5) Full **artifact file list** from `plan.md` (clarification.md, market_research.md, …) with path pattern column.</action>
  <verify>Table lists every file from plan.md delivery/output table with a path pattern.</verify>
  <done>P0-T4 satisfied; CONTEXT artifact rules reflected.</done>
</task>

<task type="auto">
  <name>Task 3: Staggered vs parallel messaging (runtime nuance OK)</name>
  <files>docs/PHASE0_DECISIONS.md</files>
  <action>Add **§ Execution ordering** covering: parallel ideal (Market + User); staggered fallback with **honest** copy (“running X, then Y”); identical output expectations; partial failure surfaces to user same as parallel. Note **small wording differences allowed** between Cursor and Claude Code if needed, with one shared bullet list of non-negotiable behaviors.</action>
  <verify>Section exists; contains explicit “honest ordering” and “same outputs” language.</verify>
  <done>Aligns with CONTEXT parallel/staggered decisions.</done>
</task>

</tasks>

<verification>
- Cross-check § Paths against `plan.md` file table (no missing outputs).
- Confirm search section mentions ttt_state recording for skip Validate.
</verification>

<success_criteria>
- [ ] Web search matrix + degradation rules documented.
- [ ] All artifact paths documented with fallback + backup + monorepo.
- [ ] Staggered/parallel UX documented with runtime wording note.
</success_criteria>

<output>
After completion, create `.planning/phases/00-runtime-gates/00-02-SUMMARY.md`.
</output>
