---
phase: 02-runtime-packaging-wave-1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/ttt/SKILL.md
  - runtime/claude-code/README.md
autonomous: true
requirements:
  - P2-T2

must_haves:
  truths:
    - "User can say 'TTT' or '/ttt' in Claude Code and the skill activates"
    - "SKILL.md triggers on moderate breadth: TTT, product spec, product management, validate idea — NOT on generic 'build' or 'brainstorm'"
    - "SKILL.md contains enough orchestration context to start TTT phases without extra setup"
    - "SKILL.md references prompts/ files by project-root-relative paths (not relative to skill directory)"
    - "Claude Code README documents three installation methods with skill install first"
  artifacts:
    - path: "skills/ttt/SKILL.md"
      provides: "Claude Code skill entry point with light orchestration"
      contains: "prompts/choreographer.md"
    - path: "runtime/claude-code/README.md"
      provides: "Installation and testing reference for Claude Code runtime"
      contains: "Install"
  key_links:
    - from: "skills/ttt/SKILL.md"
      to: "prompts/choreographer.md"
      via: "Step 1: Load the Choreographer instruction"
      pattern: "prompts/choreographer\\.md"
    - from: "skills/ttt/SKILL.md"
      to: "prompts/*.md"
      via: "Subagent prompts table mapping prompt file → agent → when used"
      pattern: "prompts/"
---

<objective>
Update the Claude Code skill entry point (`skills/ttt/SKILL.md`) to match Phase 2 decisions and create `runtime/claude-code/README.md` for installation and testing.

Purpose: Enable TTT activation in Claude Code via skill description matching. The existing 171-line SKILL.md draft is close — adjust trigger breadth, confirm path references, and verify orchestration instructions. Create a companion README for installation guidance.

Output: Updated `skills/ttt/SKILL.md` (150-200 lines) and new `runtime/claude-code/README.md` (100-150 lines).
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@skills/ttt/SKILL.md
@prompts/choreographer.md
@docs/PHASE0_DECISIONS.md
@.planning/phases/02-runtime-packaging-wave-1/02-RESEARCH.md
@.planning/phases/02-runtime-packaging-wave-1/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skills/ttt/SKILL.md</name>
  <files>skills/ttt/SKILL.md</files>
  <action>
Update the existing `skills/ttt/SKILL.md` (currently 171 lines). This is an UPDATE, not a rewrite — preserve the existing structure which is already sound. Target: 150-200 lines.

**Frontmatter changes:**

Current description:
```
description: Product management for solo builders. Turns a raw idea into a complete, validated product spec before a single line of code is written. Runs structured phases — clarify, validate, define, specify — with research-backed rigor and a momentum-protecting escape hatch.
```

Replace with a shorter, trigger-optimized description (under 250 chars, front-load key terms per research):
```
description: Product management for solo builders. Turns a raw idea into a validated product spec. Use when user says TTT, product spec, product management, or validate idea.
```

This is 178 characters — under the 250-char truncation limit. Front-loads the use case. Includes the decided moderate trigger terms.

**"When to use" section changes:**

Current triggers are too broad. Remove:
- `"build"` — fires on too many coding conversations
- `"brainstorm"` — too generic
- `"What should I build?"` — overly broad
- `"Help me figure out what to build"` — overly broad
- `"Vibe it!!"` — only makes sense mid-TTT session, not as an activation trigger

Keep/add to match decided moderate breadth:
- User says "TTT", "To The T"
- User says "product spec", "product management", "validate idea"
- User has a raw product idea and wants to validate and spec it before building
- User wants market research, user research, or competitive analysis for a product idea

**"When not to use" section:**
Current content is fine. Keep as-is.

**"Orchestration instructions" section:**

Verify all file references use project-root-relative paths (e.g., `prompts/choreographer.md`, NOT `../../prompts/choreographer.md`). Claude Code runs with the project root as working directory, so project-root-relative paths resolve correctly.

**"Runtime notes" section:**

Update the "Claude Code" subsection to be more specific about Claude Code execution:
- Subagent work: Claude Code may support `context: fork` for isolated subagent execution. If not available, run subagent prompts sequentially in the current context.
- Sequential execution is acceptable — use honest progress messaging ("Running Market Researcher first, then User Researcher").
- Same output files and quality gates regardless of execution method.

**"Architecture reference" section (bottom):**

Remove or simplify references to `ttt_master_architecture_part1.md` and `ttt_master_architecture_part2.md` — these are development-time documents, not user-facing. Replace with:
- Runtime decisions: `docs/PHASE0_DECISIONS.md`
- State schema: `schemas/ttt_state.example.json`

**Per locked decision on duplication tolerance:** Some orchestration logic in SKILL.md duplicating choreographer content is acceptable. The existing structure (phases, orchestration steps, subagent table, key rules) is the right level of duplication. Don't strip it down to a stub. Only structural changes (phase additions, gate changes) require keeping SKILL.md in sync with choreographer changes.

**Do NOT change:**
- Overall document structure (it's already sound)
- "What TTT produces" section (artifact table is correct)
- "How it works" section (architecture diagram and phase descriptions are correct)
- "Subagent prompts" section (table is correct)
- "Key rules" section (non-negotiables are correct)
  </action>
  <verify>
- `skills/ttt/SKILL.md` exists and is valid markdown
- Frontmatter description is under 250 characters and includes trigger terms (TTT, product spec, product management, validate idea)
- "When to use" section does NOT include "build" or "brainstorm" as standalone triggers
- All file references use project-root-relative paths (no `../../` prefixes)
- "Runtime notes > Claude Code" subsection has specific execution guidance
- Total lines between 150-200
- Subagent prompts table references all 7 prompt files
  </verify>
  <done>`skills/ttt/SKILL.md` updated with moderate trigger breadth in frontmatter and "When to use", project-root-relative paths confirmed, Claude Code-specific execution notes updated, 150-200 lines total.</done>
</task>

<task type="auto">
  <name>Task 2: Create runtime/claude-code/README.md</name>
  <files>runtime/claude-code/README.md</files>
  <action>
Create the directory `runtime/claude-code/` if it doesn't exist.

Create `runtime/claude-code/README.md`. Target: 100-150 lines. Writing style: agent-first (directive and precise — instruction document for the AI agent, not a user guide).

**Structure:**

1. **Title** — "TTT — Claude Code Runtime"

2. **Quick-start** (3-5 numbered steps):
   1. Install TTT as a skill (see Installation below)
   2. Start a Claude Code session in a project
   3. Say "TTT" or use `/ttt` — the skill activates from description matching
   4. If the agent responds with the Clarify phase intro, TTT is working
   5. If resuming: the agent reads `ttt/ttt_state.json` and picks up where you left off

3. **Installation** — Three methods per locked decision (skill install first and most prominent):

   **Method 1: Skill install (recommended)**
   - For project-local: copy `skills/ttt/SKILL.md` and `prompts/` directory into the project
   - Claude Code auto-discovers skills from `skills/` or `.claude/skills/` directories
   - Ensure `prompts/` and `schemas/` directories are also present at project root

   **Method 2: Clone the repo**
   - `git clone <repo-url>` — the repo layout mirrors user layout
   - Core files (`.cursor/`, `skills/`, `prompts/`, `schemas/`) are ready to use
   - Extra files (docs, evals, planning) coexist but don't interfere

   **Method 3: Manual copy**
   - Copy `skills/ttt/SKILL.md`, `prompts/` directory, and `schemas/` directory into project
   - Maintain the same relative directory structure

4. **How activation works:**
   - Claude Code reads SKILL.md description to decide relevance
   - Triggers: "TTT", "product spec", "product management", "validate idea"
   - Manual invocation: `/ttt` slash command
   - Auto-invocation: when Claude Code determines the description matches user intent

5. **Subagent execution:**
   - SKILL.md instructs the agent to load `prompts/choreographer.md` for full orchestration
   - Subagent prompts are in `prompts/` — choreographer spawns them as needed
   - If `context: fork` is available, use it for subagent isolation
   - Otherwise: sequential execution in current context is acceptable
   - Honest progress messaging: tell user which agent runs first
   - Same output files and quality gates regardless of execution method

6. **Web search:**
   - Same behavior as Cursor (per Phase 0 decisions)
   - Recommended for Validate phase
   - Graceful degradation: keep section structure with N/A, escalate after one blocked failure

7. **Updating:**
   - Primary: reinstall the skill (copy updated files)
   - Secondary: `git pull` if using clone method
   - Only structural changes (phase additions, gate changes) require SKILL.md updates — minor choreographer edits don't affect SKILL.md

8. **Verification:**
   - Say "TTT" — if Clarify phase intro appears, skill is working
   - Check that `prompts/choreographer.md` is readable from the project root
   - Verify `schemas/ttt_state.example.json` is accessible

9. **Troubleshooting:**
   - Skill not activating: check `skills/ttt/SKILL.md` exists in project, description is intact
   - Path errors: ensure `prompts/` directory is at project root (Claude Code resolves from project root)
   - Missing prompts: verify all 7 prompt files exist in `prompts/`

**Do NOT include:**
- Cursor-specific instructions (that's in runtime/cursor/README.md)
- Full orchestration logic (that's in choreographer.md, loaded by SKILL.md)
- Detailed phase-by-phase instructions
  </action>
  <verify>
- File exists at `runtime/claude-code/README.md`
- Has Quick-start section with numbered steps
- Documents three installation methods with skill install first
- Has sections for activation, subagent execution, web search, updating, verification, troubleshooting
- Writing style is agent-first (directive, not explanatory)
- References `skills/ttt/SKILL.md` and `prompts/` directory
- Total lines between 100-150
- Does NOT contain Cursor-specific content
  </verify>
  <done>`runtime/claude-code/README.md` exists with quick-start + installation (3 methods, skill install first) + reference structure, agent-first writing, 100-150 lines, covering activation, execution, web search, updating, verification, and troubleshooting.</done>
</task>

</tasks>

<verification>
1. `skills/ttt/SKILL.md` frontmatter has updated description with moderate trigger breadth
2. SKILL.md "When to use" excludes overly broad triggers ("build", "brainstorm")
3. All SKILL.md file references are project-root-relative
4. `runtime/claude-code/README.md` documents all three installation paths
5. Both files use agent-first writing style
6. Neither file contains Cursor-specific content
7. No file overlap with Plan 01 or Plan 03
</verification>

<success_criteria>
- P2-T2 satisfied: Claude Code entry point (`skills/ttt/SKILL.md`) exists with same behavioral contract as Cursor, documented in `runtime/claude-code/README.md`
- SKILL.md activates on moderate trigger set (TTT, product spec, product management, validate idea)
- SKILL.md contains entry-point + light orchestration (not a thin stub, not a full duplicate)
- Installation guide covers three paths with skill install primary
- Verification test: "Say TTT — if Clarify phase starts, it's working"
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-packaging-wave-1/02-02-SUMMARY.md`
</output>
