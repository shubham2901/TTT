---
phase: 02-runtime-packaging-wave-1
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - runtime/INVOCATION.md
  - runtime/README.md
autonomous: true
requirements:
  - P2-T3

must_haves:
  truths:
    - "A single document maps every TTT phase to the correct prompt file, input files, output file, and whether subagents run in parallel"
    - "Runtime-specific differences (Cursor vs Claude Code) are explicitly tabulated side-by-side"
    - "runtime/README.md links to all runtime-specific docs, INVOCATION.md, and lists future adapters"
  artifacts:
    - path: "runtime/INVOCATION.md"
      provides: "Unified invocation matrix for both runtimes"
      contains: "Phase"
    - path: "runtime/README.md"
      provides: "Index linking all runtime documentation"
      contains: "INVOCATION.md"
  key_links:
    - from: "runtime/INVOCATION.md"
      to: ".cursor/rules/ttt.mdc"
      via: "Cursor entry point reference in runtime differences table"
      pattern: "ttt\\.mdc"
    - from: "runtime/INVOCATION.md"
      to: "skills/ttt/SKILL.md"
      via: "Claude Code entry point reference in runtime differences table"
      pattern: "SKILL\\.md"
    - from: "runtime/INVOCATION.md"
      to: "prompts/*.md"
      via: "Phase→Agent→Files matrix mapping all prompt files"
      pattern: "prompts/"
    - from: "runtime/README.md"
      to: "runtime/cursor/README.md"
      via: "Direct link in index"
      pattern: "cursor/README\\.md"
    - from: "runtime/README.md"
      to: "runtime/claude-code/README.md"
      via: "Direct link in index"
      pattern: "claude-code/README\\.md"
---

<objective>
Create the unified invocation matrix (`runtime/INVOCATION.md`) and update the runtime index (`runtime/README.md`). The invocation matrix is a single-source-of-truth document mapping every user action to the correct prompt file and expected outputs across both runtimes.

Purpose: Satisfy P2-T3 — any agent (or human) can look at one table and know: for a given TTT phase, which prompt file runs, what inputs it needs, what output it produces, and whether it runs in parallel.

Output: `runtime/INVOCATION.md` (table-based matrix) and updated `runtime/README.md` (expanded index).
</objective>

<execution_context>
@/Users/shubham/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shubham/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@prompts/choreographer.md
@docs/PHASE0_DECISIONS.md
@.planning/phases/02-runtime-packaging-wave-1/02-RESEARCH.md
@.planning/phases/02-runtime-packaging-wave-1/02-CONTEXT.md
@.planning/phases/02-runtime-packaging-wave-1/02-01-SUMMARY.md
@.planning/phases/02-runtime-packaging-wave-1/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runtime/INVOCATION.md</name>
  <files>runtime/INVOCATION.md</files>
  <action>
Create `runtime/INVOCATION.md`. Writing style: agent-first (directive, precise). Format: table-based matrix per locked decision and research recommendation — tables are the most agent-parseable format.

**Structure:**

1. **Title** — "TTT — Invocation Matrix"

2. **One-line purpose** — "Maps every TTT phase to prompt files, inputs, outputs, and runtime-specific invocation."

3. **Table 1: Phase → Agent → Files Matrix**

Complete table covering ALL phases and agents. Columns: Phase | Agent | Prompt File | Input Files | Output File | Parallel?

| Phase | Agent | Prompt File | Input Files | Output File | Parallel? |
|-------|-------|-------------|-------------|-------------|-----------|
| Clarify | Choreographer | `prompts/choreographer.md` | (user input) | `clarification.md` | N/A (direct) |
| Validate | Market Researcher | `prompts/market_researcher.md` | `clarification.md` | `market_research.md` | Yes (with User Researcher) |
| Validate | User Researcher | `prompts/user_researcher.md` | `clarification.md` | `user_research.md` | Yes (with Market Researcher) |
| Validate | Choreographer | `prompts/choreographer.md` | `market_research.md`, `user_research.md` | (synthesis — presented to user) | N/A (after both researchers) |
| Define | Choreographer | `prompts/choreographer.md` | `clarification.md`, `market_research.md`, `user_research.md` | `definition.md` | N/A (direct) |
| Specify W1 | Product Detailer | `prompts/product_detailer.md` | `definition.md`, `market_research.md`, `user_research.md` | `solution.md` | No (runs first) |
| Specify W2 | Tech Architect | `prompts/tech_architect.md` | `definition.md` | `tech_architecture.md` | Yes (with Design + Test) |
| Specify W2 | Design Advisor | `prompts/design_advisor.md` | `definition.md`, `solution.md` | `design_guideline.md` | Yes (with Tech + Test) |
| Specify W2 | Test & Eval Gen | `prompts/test_eval_generator.md` | `definition.md`, `solution.md` | `test_eval.md` | Yes (with Tech + Design) |
| Specify | Choreographer | `prompts/choreographer.md` | all Specify outputs | `coding_agent_prompt.md`, `blueprint.md`, `versions.md` | N/A (final assembly) |
| Launch | Choreographer | `prompts/choreographer.md` | all prior outputs | `launch.md` | N/A (optional) |

Note: All output files are written to `{artifact_root}/` (default: `ttt/`). Input file paths are also relative to `{artifact_root}/` except prompt files which are at project root.

4. **Table 2: Runtime-Specific Invocation**

Side-by-side comparison of how each action works in Cursor vs Claude Code. Columns: Action | Cursor | Claude Code

| Action | Cursor | Claude Code |
|--------|--------|-------------|
| Entry point file | `.cursor/rules/ttt.mdc` | `skills/ttt/SKILL.md` |
| Start TTT | Say "TTT" in Agent chat | Say "TTT" or `/ttt` |
| Activation mechanism | Agent-decided (description match) + glob (`ttt/**`) | Description matching + `/ttt` slash command |
| Load choreographer | Agent reads `@prompts/choreographer.md` | Agent reads `prompts/choreographer.md` |
| Spawn subagent | Task tool (foreground) | Sequential in-context (or `context: fork` if available) |
| Parallel subagents | Multiple Task calls in one response | Sequential with honest progress messaging |
| Subagent fallback | Read prompt inline with `@prompts/[agent].md` | Read prompt inline |
| File path syntax | `@` paths (e.g., `@prompts/choreographer.md`) | Project-root-relative (e.g., `prompts/choreographer.md`) |
| Web search | Partial (per Phase 0) — escalate after 1 blocked failure | Partial (per Phase 0) — same escalation |
| State persistence | `ttt/ttt_state.json` | `ttt/ttt_state.json` |
| Resume session | Agent reads state file on session start | Agent reads state file on session start |
| Install method | Copy `.cursor/rules/ttt.mdc` + `prompts/` | Copy `skills/ttt/SKILL.md` + `prompts/` |
| Setup docs | `runtime/cursor/README.md` | `runtime/claude-code/README.md` |

5. **Behavioral contract (shared)** — Brief section confirming both runtimes honor the same non-negotiable behaviors per Phase 0 decisions:
   - Honest ordering when subagents run sequentially
   - Identical artifact set and structure regardless of runtime
   - No silent skip of Validate
   - Search-blocked → fast user escalation
   - State persisted in `ttt_state.json` on every transition
   - Scope guard: max 5 V1 features

6. **Vibe it!! path** — Brief note: available after Clarify in both runtimes. Choreographer generates all remaining files directly, no subagents, no quality gates. Same behavior in both runtimes.

7. **References** — Links to:
   - Phase 0 decisions: `docs/PHASE0_DECISIONS.md`
   - Choreographer (full orchestration): `prompts/choreographer.md`
   - State schema: `schemas/ttt_state.example.json`
  </action>
  <verify>
- File exists at `runtime/INVOCATION.md`
- Table 1 covers all 7 agents + choreographer across all phases (at least 11 rows)
- Table 2 covers at least 10 runtime-specific differences
- Both tables are valid markdown tables
- All prompt file references match actual files in `prompts/`
- All output file references match `docs/PHASE0_DECISIONS.md` artifact table
- References `.cursor/rules/ttt.mdc` and `skills/ttt/SKILL.md` (created in Plans 01 and 02)
  </verify>
  <done>`runtime/INVOCATION.md` exists with two complete tables: phase→agent→files matrix (all agents, all phases) and runtime-specific invocation differences (Cursor vs Claude Code, 10+ rows), plus shared behavioral contract and references.</done>
</task>

<task type="auto">
  <name>Task 2: Update runtime/README.md</name>
  <files>runtime/README.md</files>
  <action>
Replace the existing 18-line stub `runtime/README.md` with an expanded index document. Target: 40-60 lines. Writing style: agent-first (directive, precise).

**Structure:**

1. **Title** — "TTT — Runtime Documentation"

2. **One-line purpose** — "Index for runtime-specific entry points, setup guides, and the unified invocation matrix."

3. **Entry points** — Table listing the two supported runtimes and their entry point files:

| Runtime | Entry Point | Setup Guide |
|---------|-------------|-------------|
| Cursor | `.cursor/rules/ttt.mdc` | `runtime/cursor/README.md` |
| Claude Code | `skills/ttt/SKILL.md` | `runtime/claude-code/README.md` |

4. **Invocation matrix** — Link to `runtime/INVOCATION.md` with one-line description: "Maps every TTT phase to prompt files, inputs, outputs, and runtime differences."

5. **Shared behavior** — One-line: "Both runtimes honor the same behavioral contract defined in `docs/PHASE0_DECISIONS.md`."

6. **Canonical prompts** — Note: "All prompts live in `prompts/`. Both runtimes reference these files — never fork prompt content."

7. **Future adapters (Wave 2/3)** — Brief list of planned runtimes NOT yet supported:
   - Windsurf (Wave 2)
   - VS Code (Wave 2)
   - Codex (Wave 2)
   - claude.ai / Claude Desktop (Wave 3 — may need UX adaptations for no persistent filesystem)
   - Anti Gravity (Wave 3)

   Note: "Same canonical prompts. Different wrapper files. No prompt changes — adapter only."

8. **References** — Links:
   - Phase 0 decisions: `docs/PHASE0_DECISIONS.md`
   - Product plan: `plan.md`
   - State schema: `schemas/ttt_state.example.json`
  </action>
  <verify>
- File exists at `runtime/README.md`
- Links to `runtime/cursor/README.md`, `runtime/claude-code/README.md`, `runtime/INVOCATION.md`
- Lists both current entry points (ttt.mdc, SKILL.md)
- Has "Future adapters" section listing Wave 2/3 runtimes
- Total lines between 40-60
- Does NOT contain detailed orchestration content
  </verify>
  <done>`runtime/README.md` is an expanded index (40-60 lines) linking to both runtime READMEs, INVOCATION.md, listing entry points, referencing shared behavioral contract, and documenting future adapter plans.</done>
</task>

</tasks>

<verification>
1. `runtime/INVOCATION.md` has complete phase→agent→files matrix
2. `runtime/INVOCATION.md` has runtime-specific differences table
3. `runtime/README.md` links to all runtime docs
4. `runtime/README.md` lists future adapters
5. Both files are accurate against Plans 01 and 02 outputs (entry point paths, activation mechanisms)
6. No file overlap with Plan 01 or Plan 02
</verification>

<success_criteria>
- P2-T3 satisfied: single table maps user action → which prompt file → expected outputs
- INVOCATION.md covers all 7 agents + choreographer, all phases, both runtimes
- runtime/README.md serves as a complete index to all runtime documentation
- Accurate cross-references to files created in Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/02-runtime-packaging-wave-1/02-03-SUMMARY.md`
</output>
